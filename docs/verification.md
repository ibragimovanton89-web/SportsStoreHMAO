# Фактические проверки этапа

## Административная панель — 11.09.2026

### Сборка и PostgreSQL

- Выполнены restore, build и test. Итоговая сборка: **0 предупреждений, 0 ошибок**. **34 теста прошли, 0 пропущено**: прежние 23 и 11 новых административных PostgreSQL-тестов.
- Restore выполнен из локального технического NuGet feed, предварительно заполненного пакетами официального nuget.org. Из-за TLS-ограничений Windows-песочницы применялся `-p:NuGetAudit=false`; проверка актуальных уязвимостей NuGet не считается пройденной. Настройки источников проекта не переключены на этот временный каталог.
- Новые тесты проверяют текущие роли/stamp/MFA, подмену статуса DTO, запрет редактирования опубликованного товара Manager, атомарное создание и повтор, конфликт версии, Manager при AutoApply=true, ручную цену, remap источника, декодирование и пределы изображений, защиту последнего Admin, приглашение без повышения существующего аккаунта, отзыв опта, SQL-проекции справочников, цикл категорий и зависимое удаление.
- Тест адаптера импорта проверяет очистку временных файлов, повтор файла/apply, запрет apply Manager, предел 10 МиБ и откат предложений/статуса при имитированном отказе сохранения аудита. Каждая интеграционная проверка работает в отдельной настоящей PostgreSQL и удаляет только собственную БД.
- В базе разработки применена новая миграция `AdministrativeAuditAndImportSnapshots`; повторный update не нашёл изменений. `has-pending-model-changes` подтвердил соответствие модели snapshot. Обновлён `docs/migrations.sql`.
- После миграции: **33 таблицы, 228 пользовательских колонок, все с комментариями**. Системный PostgreSQL `xmin` не учитывается как пользовательская колонка.
- Пользовательские **4 405 SupplierOffer, 13 215 SupplierOfferPrice и история закупочных цен сохранены**, контрольные суммы до/после совпали. В пользовательской БД **0 Product**, тестовые карточки, сотрудники и демонстрационные наценки туда не добавлялись.

### Настоящий браузер и HTTPS

Проверка выполнена в headless Microsoft Edge через Playwright, с отдельной базой `sportsstore_browser_test_*`, отдельными файлами изображений и постоянными ключами Data Protection. Встроенные CUA/Node MCP-инструменты не запускались (`failed to write kernel assets`). Прямой TLS Kestrel в песочнице отклонял Schannel с `SEC_E_NO_CREDENTIALS`; для теста использован локальный TLS-прокси OpenSSL на 127.0.0.1:7280 перед Kestrel на 127.0.0.1:7281. Браузер общался по HTTPS; принят только самоподписанный тестовый сертификат. HTTP не использовался как замена защищённого браузерного входа.

Фактически пройдены:

1. Интерактивный bootstrap Admin со скрытым вводом пароля и явным подтверждением оператора.
2. Вход, первоначальная настройка стандартного TOTP, получение десяти резервных кодов и повторный вход с TOTP. Исправлены выявленные браузером обновление cookie после создания ключа и сохранение MFA claim при валидации stamp.
3. XLS загружен через InputFile: дата 28.07.2026, **4 405 товаров, 559 неизвестных остатков, 0 ошибок**. Preview и применение выполнены через панель, с сохранённым отчётом и пагинацией по 25 строк. Каталог не создан импортом.
4. Для `00024387` созданы свои бренд/категория, черновик и SKU `SS-00024387`. Подтверждён коэффициент 1, выбран источник закупки, загружена фотография. Это исключительно демонстрационные данные отдельной БД.
5. Через UI выбраны демонстрационный тариф «Мелкий опт», наценки 30% для розницы и 20% для опта от 5 единиц. Получены 6 022,90 RUB и 5 559,60 RUB; предложения применены отдельно. Эти значения не являются настройками магазина владельца.
6. Карточка опубликована. Сохранение из второй вкладки отклонено по Version; введённое значение осталось в форме. Причины отсутствия розничной цены/изображения до публикации отображались.
7. Через UI созданы и приняты приглашения сотрудников; повторное принятие ссылки отклонено. Новый Manager подключил MFA, включая повторную проверку финального транзакционного сценария настройки.
8. Manager не видит кнопку публикации, прямой `/admin/settings` отклоняется, попытка сохранить опубликованную карточку отклоняется сервером. После блокировки HTTP-доступ возвращает redirect к входу, старая интерактивная вкладка теряет доступ. После снятия роли сотрудника и назначения Customer доступ также отклонён.
9. POST выхода без antiforgery-токена возвращает **400**. Cookie `__Host-SportsStore.Auth`: **Secure, HttpOnly, SameSite=Lax**. Гость перенаправляется на вход, защищённое изображение гостю не выдаётся.
10. Смена собственного пароля, выход и вход резервным кодом прошли. Внешний returnUrl после входа оставляет браузер на локальном `/admin`. После пяти неверных паролей Manager заблокирован; правильный пароль во время lockout не принимается, статус виден Admin.
11. Текст под видом XLS и SVG под видом PNG отклонены. После загрузок каталог временных файлов импорта пуст. Добавление, изменение порядка и удаление фотографии прошли. Изображение сохранилось после перезапуска Web и выдавалось с HTTP 200.
12. Проверены обзор, предложения с длинным названием, карточка, импорт, цены, правила, настройки, сотрудники, журнал и пустое состояние покупателей. В финальной интерактивной проверке ошибок JavaScript/CSP нет. CSP содержит конкретный `wss://127.0.0.1:7280`, без unsafe-inline.
13. Визуально просмотрены размеры 1440, 900 и 390 пикселей. Широкие таблицы прокручиваются внутри блока; внешнего горизонтального переполнения на 900/390 нет. Снимки: [обзор](images/admin-overview.png), [карточка](images/admin-product.png), [планшет](images/admin-tablet.png), [узкий экран](images/admin-mobile.png).

### Границы проверки и эксплуатации

Прямой Kestrel HTTPS на Windows вне песочницы с пользовательским dev-сертификатом в этой сессии не проверен: указанная выше TLS-проблема относится к среде агента. Боевые сертификат, домен, хранилище ключей, отправка почты, резервное восстановление аккаунтов и нагрузочное тестирование здесь не проверялись. Реальные платёжные операции не реализованы. Исчерпывающий внешний security-аудит и NuGet vulnerability audit не заявляются.

Публичное восстановление пароля, перевыдача утраченного приглашения, сброс действующей MFA и возврат ручной цены к автоматической не добавлены. Новый bootstrap на базе владельца должен выполнить сам оператор; тестовый пароль в проект и инструкции не включён.

Браузер, тестовые Kestrel и TLS-прокси остановлены. Финальное удаление отдельной браузерной БД и временных ключей/файлов отклонено политикой разрешений среды (`approval required by policy`, подтверждение shell-команд отключено). Поэтому эта изолированная БД и технический каталог `work/admin-browser` сохранены. Основная база магазина не является целью очистки. Автоматизированные 34 теста очистили собственные базы штатно.

Следующие разделы сохраняют результаты предыдущего этапа базы/импорта; они не подменяют перечисленные выше текущие проверки.

## Подтверждено
- Исходник: Downloads/Price-list_BallMarket.ru.xls, SHA-256 D0E0B9B1A49EA69ACB1397DC298D566C32859120FD912B34E82D04882DA16C59. Файл не менялся.
- Независимое чтение xlrd и реальный C# ExcelDataReader: дата 2026-07-28, RUB, 4694 строки, 4405 товаров, 13215 цен, 559 неизвестных остатков, ошибок парсинга 0.
- Docker запущен пользователем через подготовленный Initialize-Local.ps1: официальный postgres:17.11-bookworm, контейнер sportsstorehmao-local-postgres-1 Healthy, порт 127.0.0.1:55432, named volume sportsstorehmao-local_postgres-data.
- Агент подключился к PostgreSQL по TCP под sportsstore. IsSuperuser=false.
- EF CLI применил InitialCommerce, EnumAndCurrencyConstraints и IntegrityGuards. Повторный database update сообщил, что база актуальна и изменений нет.
- 23/23 теста прошли без пропусков: 14 unit + 9 на настоящем PostgreSQL. Каждый интеграционный тест создавал отдельную БД sportsstore_test_<guid>, применял миграции дважды и удалял только её.
- Проверены идемпотентность, изменение одной цены и история, пропавшие предложения/пустые цены, откат по CHECK, конкурентное применение, устаревший preview и документ, xmin и складские ограничения, цикл категорий, оптовый статус, количественные ступени, ручная цена, устаревшие предложения расчёта, неизвестный перевод единиц и снимок заказа.
- Preview исходного файла сохранил 4405 новых позиций в staging; действующих предложений и цен до apply оставалось 0.
- Apply партии 82ef557c-c3ca-4248-beb3-7d108f25690b завершён. В базе разработки: 4405 SupplierOffer, 13215 SupplierOfferPrice, 13215 SupplierPriceHistory, 1 ImportBatch, 0 Product, 0 опубликованных товаров.
- Повторный apply и preview идентичного файла вернули ту же применённую партию. Дубли и лишняя история не появились.
- Последние dotnet restore и dotnet build --no-restore успешны: 0 ошибок, 0 предупреждений. Из-за Windows TLS restore использовал временный локальный источник официальных NuGet-пакетов и NuGetAudit=false; конфигурация проекта продолжает использовать nuget.org. Онлайн-аудит уязвимостей не выполнен.
- EF has-pending-model-changes: изменений нет. Сгенерирован идемпотентный docs/migrations.sql.
- docker compose config --quiet прошёл.
- Web-главная проверена локальным HTTP: 200, исходное содержимое и защитные заголовки сохранены; CSS 200.

## Разделение действий и ограничения среды
MCP-инструментов управления Docker не обнаружено. Пользователь разрешил Docker CLI. Прямой доступ агента к Windows named pipe Docker Engine заблокирован; пользователь выполнил запуск Docker вне песочницы. Миграции, импорт и тесты агент выполнил через .NET CLI и TCP к контейнеру.

Системный Data Protection key ring и HTTPS-сертификат недоступны в песочнице. Web smoke test использовал существующий development ephemeral mode по HTTP. HTTPS handshake и визуальная проверка не проводились.

## Завершающие проверки
- До рестарта сохранены счётчики и контрольные суммы всех предложений, текущих цен, истории и staging.
- Пользователь выполнил restart postgres и up -d --wait. Агент независимо подтвердил новое время старта PostgreSQL: 2026-09-10 21:47:24 UTC вместо 21:43:25 UTC. Счётчики и контрольные суммы всех четырёх наборов совпали: 4405 предложений, 13215 цен, 13215 записей истории; staging также сохранён.
- pg_dump/pg_restore реально выполнены по TCP с клиентом PostgreSQL 17.6 в отдельную временную БД sportsstore_restore_<guid>, принадлежащую sportsstore. Все счётчики и контрольные суммы совпали с исходной БД. Временная БД удалена после проверки; рабочая БД не изменялась.
- Проверки этапа базы данных и импорта завершены. Docker остаётся запущенным с сохранёнными закупочными предложениями. Это не означает готовность полного интернет-магазина или прохождение production-аудита.

## Повторяемая полная проверка
```powershell
./scripts/Verify-Local.ps1 -PriceFile "C:\Users\ibrag\Downloads\Price-list_BallMarket.ru.xls"
```
Скрипт останавливается при первой ошибке. Он запускает Compose, восстанавливает инструменты/пакеты, собирает решение, применяет миграции дважды, выполняет тесты, preview/apply/reapply, проверяет дубликат SHA, перезапускает контейнер и сравнивает счётчики.

## Закупочный сценарий — 11 сентября 2026

Реализованы и применены к локальной основной БД миграции `20260911174207_SupplierPurchasingAndReceiving` и `20260911180503_PreserveReceivedSupplierUnit`. Повторный `database update` не выполнил изменений, `has-pending-model-changes` не нашёл расхождений. Перед обновлением сохранён локальный pg_dump вне исходников. Контрольные суммы SupplierOffer, SupplierOfferPrice, SupplierPriceHistory и staging до/после совпали: 4 405 предложений, 13 215 цен. Существующая собственная карточка сохранена; тестовые закупки в основную БД не добавлялись. Проверены комментарии: 37 таблиц, 269 пользовательских колонок, пропущенных комментариев нет; четыре новые системные xmin также описаны.

`dotnet restore` выполнен через ранее подготовленный локальный NuGet-кеш официальных пакетов (NuGetAudit=false из-за ограничений сетевой среды); настройки источника проекта не заменены. `dotnet build --no-restore`: 0 ошибок, 0 предупреждений. Проверка уязвимостей пакетов онлайн этим запуском не выполнялась.

`dotnet test`: **41 пройден, 0 ошибок, 0 пропусков**, включая семь новых сценариев закупки на отдельных настоящих PostgreSQL. Проверены три уровня цен и ведущие нули; сохранение и повторное открытие подборки; пороги и сумма; изменение созданного заказа; историческая цена после обновления прайса; частичная/повторная/конкурентная приёмка; откат по реальному ограничению PostgreSQL; перенос принятого остатка в карточку один раз; добавление следующей поставки; неизвестные цены и единицы; независимость принятой единицы от нового прайса; права Manager; наценка 12,345% и округление; публикация отдельным публичным сервисом. Все прежние проверки импорта, Identity, цен, заказов покупателей и ограничений также прошли.

В настоящем Edge проверен отдельный HTTPS-хост с синтетическим сотрудником и отдельной БД. TLS для теста завершался локальным Node.js/OpenSSL-прокси; основная конфигурация сайта не ослаблялась. Выполнены вход с TOTP, импорт всего исходного файла (4 405 позиций, 559 неизвестных остатков, 0 ошибок), поиск 00024387, `+ / −`, переход от 4 633 ₽ к 4 325 ₽ при выборе крупного опта, сохранение ручного тестового порога, создание/передача/«В пути», создание карточки, выбор бренда и категории, фото, наценка 20% → 5 190 ₽, публикация и просмотр гостем. Приёмка сменила «В пути» на «На складе: 1 шт». Повторно проверены ввод процента без ухода из поля (25% → 5 406,25 ₽) и фиксированная цена 5 500 ₽.

Черновик скрыт от гостя, его изображение возвращает 404; опубликованное изображение — 200. Проверено запрещённое удаление последней фотографии опубликованного товара. Ошибок JavaScript не было. На ширине 390 px закупка, склад и каталог не имеют горизонтального переполнения страницы; широкая таблица прокручивается внутри контейнера.

Снимки синтетической среды: [подбор](images/purchase-selection.png), [карточка на сайте](images/purchase-public-product.png), [закупка на телефоне](images/purchase-order-mobile.png), [склад на телефоне](images/purchase-stock-mobile.png), [каталог на телефоне](images/purchase-catalog-mobile.png). Название DEMO, тестовая иллюстрация и тестовые суммы не являются реальной ценовой политикой магазина.

Ограничения: внешняя отправка заказа, возвраты/исправления уже проведённой приёмки, складские движения между складами и покупательский checkout не реализованы. Отмеченный статус передачи сам не отправляет сообщение поставщику. Выход в интернет, нагрузочное тестирование и оплата не проверялись этим этапом.

После браузерного сценария повторены preview и apply идентичного исходного файла: сохранились 4 405 предложений и 13 215 цен; контрольные суммы закупочного заказа, его строк, собственного остатка и цены продажи 5 500 ₽ не изменились. Тестовый Edge и сервер остановлены, созданная этим этапом браузерная БД удалена; прежние базы и контейнеры пользователя не удалялись.

## Исправление подключения Visual Studio

Удалён шаблонный Development-порт 5432. Если явная строка подключения отсутствует, Web в Development читает локальный .env корня решения; окружение и User Secrets имеют приоритет, Production этот файл не читает. Проверено подключение роли sportsstore к sportsstorehmao на порту из .env и запуск Web без ConnectionStrings__DefaultConnection: GET /catalog вернул HTTP 200 без NpgsqlException. Решение собрано в отдельный выходной каталог: 0 ошибок и предупреждений; обычные DLL удерживались текущим пользовательским запуском Visual Studio. Проверочный сервер остановлен.

## Объединение разделов управления — 12 сентября 2026

Убраны отдельные пункты карточек и предложений. Единый склад показывает публикацию и наличие, включая карточки без вариантов; закупки и прайс доступны как представления «Мои заказы». Обновлены ссылки редакторов и обзора, старые списки перенаправляются с параметрами поиска. Схема БД не менялась. На изолированной PostgreSQL прошли три проверки: новый сценарий пустой карточки и фильтров публикации, существующий расчёт закупки и полный сценарий приёмки/публикации. Проверка браузером этого изменения не выполнялась.

## Остаток поставщика в подборе

Отдельный список прайса убран; старые ссылки ведут в «Мои заказы». В подбор добавлены исходный и доступный для текущего заказа остатки. Известный ноль и ручное превышение проверяются сервером, неизвестный остаток не превращается в ноль. Сборка: 0 ошибок и предупреждений. Три целевые проверки PostgreSQL прошли: изменение доступного остатка при выборе/удалении, запрет превышения и нулевого остатка, неизвестное наличие, падение остатка после обновления и возврат переданного заказа для исправления; прежний расчёт сумм также проверен. Схема БД и пользовательские остатки не изменялись. Браузерная проверка этого изменения не выполнялась.

## Проверка обновления интерфейса админки — 12.09.2026

- `dotnet restore` из локального кэша пакетов: успешно; онлайн-аудит NuGet в этой проверке отключён из-за ограниченного сетевого доступа.
- `dotnet build SportsStoreHMAO.sln --no-restore`: 0 ошибок, 0 предупреждений; отдельный OutputPath, чтобы не останавливать пользовательский Visual Studio.
- Полный набор `SportsStore.Tests`: 49 пройдено, 0 пропущено. Интеграционные тесты используют отдельные PostgreSQL-базы. Новый тест проверяет регистр, поиск по началу названия, границу страницы 25 записей и сохранённый выбор вне страницы.
- Настоящий Edge, отдельная тестовая PostgreSQL-база, синтетический Admin с TOTP: проверены живой поиск брендов, раскрывающийся список, сохранение ADIDAS и повторное чтение карточки, поиск позиции 00024387 в сборке заказа. Ошибок JavaScript не зарегистрировано.
- Проверены размеры 1440×1000 и 390×844. На мобильном документ имеет ширину 390 px; широкая таблица прокручивается внутри блока. Скриншоты: `images/admin-product-desktop.png`, `images/admin-purchase-desktop.png`, `images/admin-purchase-mobile.png`.
- Основная база и запущенное пользователем приложение не изменялись в ходе визуальных проверок. Миграция не требуется.

## Перенос цен в карточку — 12.09.2026

`dotnet restore` из локального кэша и сборка решения прошли успешно, 0 предупреждений и 0 ошибок. Проверяются независимые розничная и оптовая цены, расчёт процента от закупки, идемпотентный повтор без лишней истории, конфликт версии, запрет Manager и сохранение количественных ступеней. Тесты работают на изолированных PostgreSQL-базах. Модель БД и реальные данные не изменялись; бизнес-сервисы импорта и история сохранены. Браузерная проверка этого изменения отдельно не выполнялась.

## Временный вход Admin в Development — 12.09.2026

- Сборка решения: 0 ошибок, 0 предупреждений.
- Четыре теста границ режима прошли: запрет Production/Staging, проверка loopback/Host и невозможность подделать серверный маркер обычными claims.
- Проверка в Edge без cookie: /admin открывается, интерактивные переходы в справочники и административные настройки работают, состояние сохраняется после 15-секундной перепроверки.
- Отдельный процесс с флагом false перенаправляет /admin на /account/login.
- Запрос с нелокальным Host отклонён фильтром AllowedHosts (400).
- Реальный запуск Production с флагом true прекращён до запуска HTTP-сервера с диагностикой запрета тестового входа.
- Проверки интерфейса выполняли только чтение данных. Пользовательский сервер Visual Studio не останавливался; собственные проверочные процессы остановлены после проверки.

## Фильтры закупочного прайса — 12.09.2026

Полный проход исходного бинарного XLS штатным C#-парсером: 4 405 товаров, 48 брендов и 125 групп; неизвестный бренд у 256 строк, неизвестная группа у 166. Проверены исходные 253 заголовка листа. Повторный импорт в основную БД не выполнялся.

Сборка решения без ошибок и предупреждений; полный набор 67 тестов пройден. Новые тесты проверяют смешанные бренды, границы слов, регистр, неизвестные признаки, совместные серверные фильтры и неизменность заказа.

В Edge на отдельной тестовой PostgreSQL-базе с полным прайсом проверены поиск начала бренда, выбор группы, пагинация, смена фильтров, сохранность количества и суммы, режим выбранных позиций, перезагрузка URL и ширина 390 px без горизонтального переполнения документа. Сделаны снимки `images/purchase-filters-desktop.png` и `images/purchase-filters-mobile.png`. Основные заказы и склад пользователя не изменялись.

## Экспорт закупочного заказа в Excel

- `dotnet restore` из локального кэша пакетов и `dotnet build`: успешно, 0 предупреждений и ошибок. Новых пакетов нет.
- Полный набор: 72 теста успешно. После уточнения формата ячеек повторно прошли 2 теста экспорта.
- PostgreSQL: экспорт 27 выбранных строк без пагинации, исторические цены после изменения предложений, ведущие нули, пустая цена, буквальный текст с `=HYPERLINK`, отсутствие внутренних примечаний, проверка Admin, версии и статуса. Повторное скачивание не изменяет версию заказа.
- ExcelDataReader прочитал готовый xlsx: одна вкладка, все строки, числовые цены и итоги, строковые коды с нулями. Формулы ограничены произведением цены на количество и итогами.
- Edge/Playwright на отдельной тестовой PostgreSQL: отметка передачи скачала файл при активном фильтре, скрывающем выбранную позицию; повторная выгрузка с тем же именем прошла, статус сохранился после перезагрузки. Рабочая БД пользователя не изменялась.
- Книга импортирована отдельным табличным движком для предпросмотра; изменение количества с 1 до 2 пересчитало строку и общий итог с 4633 до 9266 RUB. Microsoft Excel/COM для проверки не запускался.

## Покупательская витрина — 14.09.2026

Проверялся текущий код этапа 3, а не старый отчёт о 72 тестах.

- `dotnet restore` из подготовленного локального NuGet-кэша успешно; новых пакетов нет. NuGetAudit был отключён для воспроизводимого offline restore: проверка уязвимостей в этот прогон не входит.
- `dotnet build` решения: **0 ошибок, 0 предупреждений**. Сборка выполнялась в отдельные `work/storefront-stage3-*/bin`, пользовательский Visual Studio не останавливался.
- Текущий полный набор: **82/82 теста**, без пропусков. Финальный прогон — 57 секунд. В него входят существующие проверки публикации, закупок, приёмки, цен, импорта, снимков заказов и новые StorefrontTests/CatalogUrlTests.
- PostgreSQL настоящий, отдельные временные базы. Миграции применялись при каждой инициализации AdminTests, затем повторно без изменений. Новых миграций этап не потребовал.
- Рабочий Docker/PostgreSQL на 55432 при проверке не отвечал. Использован имеющийся PostgreSQL runtime: отдельный кластер на 55434 для браузера и проверок витрины при LC_CTYPE=C; отдельный кластер на 55435 с ICU для полного набора. Рабочая БД, закупки, цены и публикации пользователя не менялись. Docker/Visual Studio не перезапускались.
- На первом полном прогоне в искусственной C-локали 3 старых административных теста выявили зависимость существующего административного поиска/сопоставления от локали. Полный набор прошёл в Unicode/ICU-окружении. Новый публичный поиск дополнительно проверен в C-локали и использует явную ICU-collation независимо от настройки базы. Административный поиск этим этапом не переписывался.

### Новые автоматические проверки

Published/Draft/Archived и фасеты; отсутствие варианта без базовой Retail-цены; отсутствие Wholesale в DTO; русский и латинский регистр; ведущие нули SKU, ManufacturerCode, буквальные `%`/`_`; родительская категория с потомками; OR/AND и размер/цвет/цена/наличие одного варианта; все четыре стабильные сортировки и границы страниц; нормализация URL и запрет внешнего return/canonical; «от» только при различии цен, включая разные единицы.

Available проверен как 10−3=7. Incoming проверен после частичного получения: (5−2)×2=6; отдельно проверены отмена, Created, неподтверждённый коэффициент, несовпадение единиц, собственный приоритет статуса, наличие поставщика и нераспределённый физический товар. Новый apply следующего синтетического прайса сохранил IsManual, Amount и xmin розничной цены; чтение сохранило xmin/OnHand/Reserved склада. История закупочной цены при этом появилась.

Перехватчик команд EF измерил одинаковые **11 SQL-чтений** для 1 и 24 карточек. Полный каталог не загружается для фильтрации в памяти. Это проверка отсутствия N+1, не нагрузочный тест большого ассортимента.

### Браузер и HTTP

Проверочный Web на `http://localhost:7293` запущен с `Development__BypassAdminAuthentication=false`, отдельной PostgreSQL-базой и отдельными файлами изображений. `/admin` этого процесса возвращает 302 на вход. Для тестовой архивации использовался отдельный локальный административный процесс 7294 с существующим Development-обходом; глобальная пользовательская настройка не изменялась.

В браузере пройдены: исходная витрина для сравнения; новая выдача; русский поиск; совместные фильтры ADIDAS/M/Красный; переход к найденному SKU за 1500 RUB; синий M за 2000; красный S за 1000 с остатком 7; запрет синего S; миниатюры и клавиатура галереи; выбор клавиатурой; возврат к фильтрам; удаление метки; Back/Forward и Refresh. При поиске артикула показан один соответствующий вариант, без ложного «от».

Проверены реальные ширины **1440, 768, 390 px** для каталога/карточки. ScrollWidth документа не превышает viewport; длинное название переносится. На телефоне панель фильтров закрывается без потери отмеченных полей, меню раскрывается. Выбранные кнопки имеют явные aria-pressed=true/false и контрастное выделение. В обычном сценарии JavaScript-ошибок не обнаружено. Намеренный тест отсутствующего файла ожидаемо дал resource 404 и подпись вместо фотографии.

HTTP-проверки:

| Сценарий | Результат |
|---|---|
| Начальный SSR каталога/карточки | 200, содержимое содержит названия, розничные цены и ссылки |
| Неизвестный Guid, неверный Guid, скрытый товар | 404 |
| Неверные page/min/category | 200 с нормализованным состоянием |
| Чужой variant, внешний return, segment=Wholesale | Собственный розничный вариант по умолчанию, без внешней ссылки и чужого SKU |
| Published → фото гостю | 200 и Cache-Control: no-store |
| Архивация тестовой карточки кнопкой админки → следующий запрос карточки/фото гостем | 404 / 404, подтверждено также браузером |
| Архивная публичная карточка сотруднику / её изображение Staff | 404 / 200 |
| Временно отсутствующий WebP | 404 и понятная замена в галерее |
| Отдельный процесс с недоступной БД | 200 со страницей ошибки загрузки, а не «каталог пуст» |

После проверки тестовая карточка снова опубликована через админку; временно переименованный тестовый WebP восстановлен. Пользовательские данные для демонстрации не использовались.

Ограничения: нет checkout/аккаунтов/оптовой витрины; no-store и оригинальные проверенные WebP сохранены, миниатюры не генерируются. Публичный production-origin ещё должен задать владелец. Массовая нагрузка, другие браузерные движки и аудит уязвимостей не входили в этот прогон. Правила и запуск — [storefront.md](storefront.md).


## Аккаунты покупателей — 14.09.2026

Финальная сборка solution выполнена последовательно (`-m:1`) в отдельный каталог: **0 ошибок, 0 предупреждений**. `git diff --check` не выявил ошибок пробелов.

### Подтверждённые проверки

- Реализация добавлена в существующие слои; миграция `20260913215736_CustomerAccountsWholesale` создаёт заявки, историю и очередь уведомлений. Рабочая пользовательская БД, реальные роли/карточки/закупки и глобальный admin bypass не изменялись.
- Restore выполнен из локального NuGet feed с `NuGetAudit=false` из-за ограничений окружения; внешний аудит уязвимостей не считается выполненным. Новых NuGet-пакетов для четвёртого этапа нет.
- Полный запуск: **98 тестов, 98 прошли, 0 пропущено**. PostgreSQL — отдельная техническая ICU-база/кластер на 55435; каждый интеграционный тест создаёт собственную БД, применяет миграции и удаляет только её. EF InMemory не использовался.
- Проверены конкурентная регистрация, отсутствие присвоения прежнего покупателя, атомарный откат Identity при отказе записи профиля, назначение только Customer, подтверждение/неверное назначение/чужой пользователь/повтор/просроченный токен, восстановление и отзыв stamp.
- Реальный 15-секундный таймер circuit проверен ожиданием: покупатель без Staff-роли сохраняет вход, блокировка отзывает состояние. Старые проверки MFA, приглашений и защиты последнего Admin продолжают проходить.
- Проверены чужие адреса и заявки, xmin-конфликт, одна Pending-заявка, идемпотентность, PostgreSQL partial unique index, гонка решения с изменением реквизитов, отзыв при смене существенных данных, сохранение опта при смене телефона, старый AdminPeople через общую историю/outbox.
- Проверены границы оптовых ступеней, отсутствие цены без розничной подмены, отзыв без нового входа, блокировка пользователя, скрытый товар и чужой вариант. Сохранены прежние проверки витрины, закупок, ручных цен и изображений.
- Outbox не виден обработчику до commit; после commit отправляется синтетическому Capture-транспорту. Сбой не повторяет решение. Проверка миграции со старой схемы сохраняет исходную версию и Approved-клиента без созданных задним числом заявок.
- На отдельной браузерной fixture-базе штатный EF CLI повторно выполнил update: изменений нет. `has-pending-model-changes`: изменений модели нет. У новых пользовательских колонок PostgreSQL нет пропущенных комментариев.

### Протокольные HTTPS-проверки

Отдельный процесс Web с `Development:BypassAdminAuthentication=false`, собственными storage/keys/Capture и синтетическими аккаунтами. Из-за невозможности Kestrel открыть TLS-ключ внутри песочницы использован отдельный loopback Node TLS-прокси 7393 → 7394 с существующим тестовым сертификатом. HTTP-клиент проверяет именно этот сертификат, а не отключает TLS-валидацию.

Проверены настоящие GET/POST: antiforgery (POST без токена → 400), регистрация с попыткой mass assignment, подтверждение письма, отказ входа до подтверждения, login → 302, собственный профиль, изменение профиля/адреса, Pending-заявка со снимком, смена пароля, отказ старой cookie, новый доступ текущей cookie, logout GET → 405 и POST → 302. Покупатель не входит в /admin; сотрудник получает MFA-переход, не входит в /admin до TOTP, после TOTP получает 200. Гостевой URL с segment=Wholesale не раскрывает оптовую цену. Итоговый Cache-Control — private, no-store даже после добавления antiforgery-cookie.

Это протокольные проверки, **не браузерный проход интерфейса**.

### Браузерный HTTPS-проход — завершён 14.09.2026

После успешного импорта сертификата пользователем и перезапуска Codex HTTPS открылся без отключения проверки сертификатов. Использована та же изолированная fixture-база, локальный Capture и `Development:BypassAdminAuthentication=false`. Покупатель работал на `localhost:7393`, отдельная MFA-сессия сотрудника — на `127.0.0.1:7393`; оба адреса входят в сертификат. Это позволило проверить отзыв опта без выхода покупателя.

- Создан синтетический покупатель; получено локальное письмо, выполнены подтверждение и вход через формы браузера.
- Сессия сохранилась дольше 15-секундной периодической перепроверки. Сохранены профиль организации, адрес и изменение адреса; реальные персональные данные не использовались.
- Заявка появилась как Pending со снимком реквизитов. Admin вошёл через отдельную форму с TOTP, одобрил заявку; состояние и публичная история обновились.
- Покупатель увидел Retail 1000 ₽ и отдельную Wholesale 777 ₽. Смена размера/цвета показала правильные розничные цены 1500/2000 ₽ и отсутствие оптовой цены без подмены прежней суммой.
- Admin отозвал опт в отдельной сессии. На уже открытой покупательской карточке кнопка обновления убрала персональный блок без повторного входа.
- Восстановление через локальное письмо сменило пароль. Прежняя cookie перестала открывать профиль; вход новым тестовым паролем успешен.
- Повторная заявка после отзыва прошла рассмотрение. В fixture добавлена явно синтетическая ступень: 9 единиц — 777 ₽, 10 — 650 ₽. Ноль выводит ошибку и скрывает сумму; повторное обновление заблокировано, исправление количества восстанавливает цену.
- На 390 px проверены профиль, адреса, история заявки, меню и карточка. Карточка также проверена на 768 и 1440 px: горизонтального переполнения документа нет. Клавиатурой проверены Tab, ввод количества и Enter для смены фотографии.
- В свежей вкладке окончательной сборки ошибок JavaScript нет. В старых вкладках были ожидаемые 502/разрывы Blazor во время перезапуска только тестового процесса; после его запуска они не воспроизводятся.

Браузерная проверка выявила и исправила два случая: причина административного решения теперь обновляется при вводе; неверное количество больше не позволяет кнопке обновления вернуть цену предыдущего количества. Финальная сборка после исправлений: 0 ошибок, 0 предупреждений. Серверная модель и миграции в этом продолжении не менялись. После исправлений повторно выполнен полный набор: **98/98**, 0 пропущено, около 1 минуты 41 секунды. Первая попытка этого повторного запуска остановилась на настройке тестов из-за отсутствующей переменной подключения приложения; после задания обеих тестовых строк подключения все проверки прошли.

Внешний SMTP не настроен и не проверен; писем реальным получателям не отправляли. Юридические тексты, домен и производственные секреты задаёт владелец. Этап не включает корзину, оформление, оплату или резервирование.

### Исправление локального входа покупателя

Обнаружена подмена действующей cookie покупателя локальным Admin при включённом bypass, приводившая к возврату из профиля на login. Middleware теперь сохраняет любую аутентифицированную identity. Проверены 7 тестов DevelopmentAdminAccessTests, включая Customer/Admin/Manager и составной principal; все прошли. Сборка solution в отдельном каталоге: 0 ошибок, 0 предупреждений. Полный набор PostgreSQL в этом исправлении повторно не запускался. Пользовательский процесс Visual Studio не перезапускался.


### Применение миграции к локальной рабочей БД

При разборе ошибки сохранения профиля обнаружено отсутствие миграции четвёртого этапа в sportsstorehmao (были применены только 7 предыдущих). Штатным EF Core CLI применена 20260913215736_CustomerAccountsWholesale; проверены запись в истории и наличие WholesaleApplication, WholesaleDecision, NotificationOutbox. Существующие пользовательские профили и телефоны вручную не изменялись. Повторное сохранение пользовательской формы остаётся за пользователем.


## Этап 5 — проверка 14.09.2026

- `dotnet restore` выполнен из локального NuGet-кэша; сетевой аудит пакетов в этом проходе отключён (`NuGetAudit=false`).
- Сборка solution .NET 10: **0 ошибок, 0 предупреждений**, отдельный каталог вывода, без остановки Visual Studio.
- Полный актуальный набор: **122/122**, без пропусков, 2 мин 6 с. После усиления фикстуры Incoming повторно пройден соответствующий PostgreSQL-тест; после уточнения текста письма повторно проверена доставка/откат очереди.
- PostgreSQL 17, порт 55435: каждый интеграционный тест создаёт изолированную БД, применяет миграции и проверяет повторное применение. EF `has-pending-model-changes`: изменений нет. Повторный `database update` браузерной БД: уже актуальна.
- Миграция `20260913231544_CustomerCartOrdersReservations` применена к рабочей локальной `sportsstorehmao` после `pg_dump -Fc` в закрытый рабочий каталог `work/stage5/before-stage5.dump`. Тестовые данные туда не добавлялись. Автомиграции Web не включены.

Новые проверки: гостевые владельцы и реальная защита cookie, повторное объединение и превышение количества без обрезания, xmin двух вкладок, снятая публикация, оптовые ступени/отсутствие цены/отзыв опта, изменение цены/адреса/цвета после preview, отсутствие повторного согласия при изменении только версии, идемпотентные ключи и владение заказом. Параллельные отдельные соединения с общим стартовым барьером проверяют одну корзину и последнюю единицу. CHECK-ошибка второй строки откатывает всю транзакцию. Проверены несколько складов, недопустимость Incoming/SupplierOffer.Stock/UnallocatedStock, отмена/продление, два обработчика истечения, перезапуск процессора, снимки и очередь до/после commit. Старые тесты закупок и оптовых уведомлений также прошли.

HTTPS-браузер: `https://localhost:7393`, отдельные БД/изображения/Data Protection/Capture, `Development:BypassAdminAuthentication=false`. Сотрудник входил с настоящей cookie и TOTP через `https://127.0.0.1:7393`, чтобы разделить сессии. Проверены:

1. Добавление конкретного S/Красный гостем, вход и перенос корзины.
2. Изменение количества; адрес, preview, создание и просмотр заказа владельцем/сотрудником.
3. Изменение тестовой оптовой цены через административную карточку (777 → 780), отказ по старому preview и повторное согласие. Ранее отдельно проверено изменение розничной цены 1000 → 1100.
4. Отмена покупателем и восстановление доступности 7 шт; подтверждение и продление сотрудником.
5. Оптовая ступень от 5: 650 × 5 = 3250; отдельный оптовый заказ.
6. Back и повтор исходной формы: возвращается тот же URL заказа, дубля нет. После обнаруженного регенерирования ключа на GET ключ закреплён за preview.
7. Ускоренный срок через настройку **только тестового процесса** `Orders:ReservationHours=0.02` (72 секунды): заказ автоматически перешёл в «Резерв истёк». Дополнительно проверен обработчик просроченной тестовой записи.
8. Ширина 390 px: исправлен длинный номер; document.scrollWidth == clientWidth (375 с полосой прокрутки), корзина/подтверждение/заказ не переполняются. Tab из количества переводит фокус на обновление. Ошибок JavaScript в покупательской вкладке не зафиксировано.
9. Capture сформировал уведомления только синтетическим example.invalid. Реальная SMTP-доставка не проверялась.

Рабочие закупки, цены, остатки и персональные данные пользователя для демонстрации не менялись. Тестовый процесс и его 72-секундный срок не являются настройкой рабочего приложения. Локальный дамп содержит рабочие данные, его нельзя публиковать вместе с исходниками.
